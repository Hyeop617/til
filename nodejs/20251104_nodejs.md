## Node.js

V8 Javascript Engine으로 브라우저 외부에서 Javascript를 실행할 수 있는 런타임 환경.

Node.js는 싱글 프로세스로 동작하고, 매 요청마다 스레드를 만들지 않음.
비동기 I/O를 사용하며, 이벤트 루프를 통해 요청을 처리함.

Node.js에서 주요 장점은 JS로 프론트엔드와 백엔드를 모두 개발할 수 있다는 점.